# Optional public settings for a separate staging project.
# Enables deliberate account sign-in; local drafts never upload automatically.
# Never put a service-role key in a VITE_ variable.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
